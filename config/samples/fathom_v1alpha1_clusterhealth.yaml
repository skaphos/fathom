apiVersion: fathom.skaphos.io/v1alpha1
kind: ClusterHealth
metadata:
  labels:
    app.kubernetes.io/name: fathom
    app.kubernetes.io/managed-by: kustomize
  name: platform
spec:
  description: "Aggregate the worst-case Result across every HealthCheck in this namespace."
  # An empty/omitted selector matches all HealthChecks in the same namespace
  # as this ClusterHealth.
